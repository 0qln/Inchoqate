<Window x:Class="Inchoqate.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Inchoqate.GUI"
        xmlns:main="clr-namespace:Inchoqate.GUI.Main"
        xmlns:titlebar="clr-namespace:Inchoqate.GUI.Titlebar"
        mc:Ignorable="d"
        Height="450" 
        Width="800"
        Style="{StaticResource BorderlessWindow}"
        MouseMove="This_MouseMove"
        MouseUp="This_MouseUp"
        Title="MainWindow"
        x:Name="E_This">

    <Window.Resources>

        <Style x:Key="K_TitlebarStyle" TargetType="titlebar:PrettyTitlebar">
            <Setter Property="Title" Value="Inchoqate"/>
            <Setter Property="Background" Value="{DynamicResource Background_2}"/>
        </Style>
        
    </Window.Resources>

    <Canvas x:Name="E_MainCanvas">

        <AdornerDecorator >


            <Grid Width="{Binding Path=Width, ElementName=E_This}"
              Height="{Binding Path=Height, ElementName=E_This}">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>


                <titlebar:PrettyTitlebar x:Name="Titlebar"
                                         Grid.Row="0"
                                         Style="{StaticResource K_TitlebarStyle}"
                                         ClipToBounds="False">

                    <titlebar:PrettyTitlebar.ActionButtons>
                        <titlebar:ActionButtonCollection>
                            <titlebar:ActionButton Title="AButton 1"/>
                            <titlebar:ActionButton Title="AButton 2"/>
                        </titlebar:ActionButtonCollection>
                    </titlebar:PrettyTitlebar.ActionButtons>
                    
                </titlebar:PrettyTitlebar>

                <Grid Grid.Row="1">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <main:PreviewImage x:Name="PreviewImage"
                                        Grid.Column="0"/>

                    <Rectangle x:Name="Seperator"
                                Grid.Column="1"
                                Fill="White"
                                MouseDown="Seperator_MouseDown"
                                MouseUp="Seperator_MouseUp"/>

                    <main:EditorInputs x:Name="EditorInputs"
                                        Grid.Column="2"
                                        Width="200"/>

                </Grid>

                <Grid Grid.Row="0" Height="20" >

                    <!--<AdornerDecorator>
                        <StackPanel>
                            <TextBlock Foreground="White" Text="aekflj"></TextBlock>
                            <TextBlock Foreground="White" Text="lakjfe"></TextBlock>
                        </StackPanel>
                    </AdornerDecorator>-->
                
                </Grid>

            </Grid>
            
        </AdornerDecorator>

    </Canvas>
    
</Window>
